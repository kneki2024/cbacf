doctype html
html
  head
    title Active Mobility Infrastructure Benefit Analysis
    link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css')
    link(rel='stylesheet', href='/css/index.css')
    link(rel='stylesheet', href='/css/adv_cal_select.css')
    link(rel='stylesheet', href='/css/param_edt.css')
  body
    include login.pug

    include terms.pug

    include basic_cal.pug

    include adv_cal_select.pug

    include param_edt.pug

    include results.pug

    a.descript-box default

  script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js")
  script(src="/js/login.js")
  script(src="/js/terms.js")
  script(src="/js/msg.js")
  script(src="/js/basic_cal.js")
  script(src="/js/adv_cal.js")
  script(src="/js/par_edit.js")
  script(src="https://cdn.jsdelivr.net/npm/echarts@5.3.2/dist/echarts.min.js")
  script(src="/js/show_charts.js")
  script.
    function toggleCustomInput(selectElem, inputId) {
      const customInput = document.getElementById(inputId);
      var $selectElem = $(selectElem);
      if (login_flag){
        $plus = $selectElem.parent().children('.fa-plus');
        $minus = $selectElem.parent().children('.fa-minus');
      }
      if (selectElem.value === 'custom') {
        customInput.style.display = 'block';
        $plus.css({'display': 'block'});
        $minus.css({'display': 'none'});
      } else {
        customInput.style.display = 'none';
        $plus.css({'display': 'none'});
        $minus.css({'display': 'block'});
      }
        
    }



    // const apiKey = 'b53768bad1mshb306639e3d6ef05p18b993jsn37375458ef37'; // Replace with your RapidAPI key

    // async function populateCountries() {
    //   const countrySelect = document.getElementById("country-select");
    //   try {
    //     const response = await fetch('https://world-geo-data.p.rapidapi.com/countries', {
    //         method: 'GET',
    //         headers: {
    //             'X-RapidAPI-Key': apiKey,
    //             'X-RapidAPI-Host': 'world-geo-data.p.rapidapi.com'
    //         }
    //     });
    //     const data = await response.json();
    //     console.log(data);  // Log the response to see its structure

    //     // Access the countries array in the response and sort it alphabetically by name
    //     const countries = data.countries.sort((a, b) => a.name.localeCompare(b.name));
    //     countries.forEach(country => {
    //         const option = document.createElement("option");
    //         option.value = country.code;
    //         option.textContent = country.name;
    //         countrySelect.appendChild(option);
    //     });
    //   } catch (error) {
    //     console.error('Error fetching countries:', error);
    //   }
    // }

    // // Initial population of countries
    // populateCountries();


          